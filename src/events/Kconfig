menu "Events"

config LIBREWATCH_PASSKEY_EVENT_INIT_LOG
    bool "Log passkey events"
    default y

config LIBREWATCH_UI_EVENT_INIT_LOG
    bool "Log UI events"
    default y

config LIBREWATCH_SENSORS_EVENT_INIT_LOG
    bool "Log sensors events"
    default y

config LIBREWATCH_MEDIA_EVENT_INIT_LOG
    bool "Log media player events"
    default y

config LIBREWATCH_DISCOVERY_EVENT_INIT_LOG
    bool "Log discovery events"
    default y

config LIBREWATCH_NOTIFICATION_EVENT_INIT_LOG
    bool "Log notification events"
    default y

config LIBREWATCH_DFU_EVENT_INIT_LOG
    bool "Log notification events"
    default y

endmenu
